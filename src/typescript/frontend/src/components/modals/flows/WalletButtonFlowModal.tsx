import { type ApiMarket } from "@/types/api";
import { BaseModal } from "../BaseModal";

type Props = {
  allMarketData: ApiMarket[];
};

export const WalletButtonFlow: React.FC<Props> = ({ allMarketData }) => {
  return <div>hi</div>;
};
