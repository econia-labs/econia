/** @type {import('prettier').Config} */
const config = {
  semi: true,
  tabWidth: 2,
  trailingComma: "es5",
  singleQuote: false,
  arrowParens: "always",
  printWidth: 80,
};

module.exports = config;
